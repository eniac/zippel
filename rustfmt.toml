# Same edition as `[workspace.package]` in Cargo.toml, so plain `rustfmt` matches `cargo fmt`.
edition = "2024"

ignore = [
    "benchmarks/src/pari_upstream",
    "benchmarks/src/sumcheck_upstream",
    "benchmarks/src/pst13_upstream",
    "benchmarks/src/hyrax_upstream",
    "benchmarks/src/ark_spartan_upstream",
]
