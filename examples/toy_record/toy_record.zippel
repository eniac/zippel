type User = { name: Field, balance: Field };

fn main() {
    let a: User = {| name: 1, balance: 100 |};
    let name = a.name;
    let balance = a.balance;
    verify(name == 1 && balance == 100);
}
