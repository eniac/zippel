proto a<F: Field>(public x: F) where true {
    t <- x + 0;
    step(t)
}

fn base<F: Field>(public hash: F) -> Bool {
    verify(1 == 1)
}

fn step<F: Field>(public hash: F) -> Bool {
    next_hash <- hash;
    base(next_hash)
}
