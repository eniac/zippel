proto schnorr<G: Group, F: Scalar<G>>(private x: F, public g: G, public h: G) where h == g*x {
  let a = random<F>;
  let b = random<F>;
  u <- g*a;
  v <- g*b;
  c <- challenge<F*>;
  z <- a + b*c + x*c*c;
  verify(g*z == u + v*c + h*(c*c))
}
